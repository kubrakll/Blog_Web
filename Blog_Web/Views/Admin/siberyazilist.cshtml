@using PagedList
@using PagedList.Mvc
@using Blog_Web.Models.classes
@model IPagedList<SiberText>
@{
    ViewBag.Title = "siberyazilist";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Title</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.SiberTextID</td>
            <td>@item.Title</td>
            <td><a href="/Admin/siberyaziGetir/@item.SiberTextID" class="btn btn-success">Güncelle</a></td>
            <td><a href="#" class="btn btn-info">Detay</a></td>
            <td><a href="/Admin/siberyaziSil/@item.SiberTextID" class="btn btn-danger">Sil</a></td>


        </tr>
    }

</table>

@Html.PagedListPager((IPagedList)Model, sayfa => Url.Action("siberyazilist", new { sayfa }), new PagedListRenderOptions
{
    LiElementClasses = new List<string>
    {"page-link"}
})

<a href="/Admin/siberyeniYazi/" class="btn btn-info">Yeni Yazı Ekle</a>


